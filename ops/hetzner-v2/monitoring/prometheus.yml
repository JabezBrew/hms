global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: hms-api
    metrics_path: /api/v2/metrics
    scheme: http
    static_configs:
      - targets:
          - hms-api:8080
        labels:
          service: hms-api
