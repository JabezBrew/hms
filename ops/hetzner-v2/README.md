# Deprecated Rust V2 Compose path

`ops/hetzner-v2/` is kept only as a compatibility shim. The reusable Rust V2
Docker Compose deployment kit now lives at `ops/compose-v2/`.

Use:

```bash
ops/compose-v2/deploy.sh
```

For current GCP staging, use:

```bash
ops/gcp-staging/deploy.sh
```
