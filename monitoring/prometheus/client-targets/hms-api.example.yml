# Copy to hms-api-<client>.targets.yml and replace the sample IP.
# Port 9188 is the private Caddy metrics proxy. It forwards /api/metrics/
# to the Rust V2 API's /api/v2/metrics endpoint.
- targets:
    - 10.90.0.11:9188
  labels:
    client: acme
    environment: production
    service: api
