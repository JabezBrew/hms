# Copy to hms-worker-<client>.targets.yml and replace the sample IP.
# Port 9188 is the private Caddy metrics proxy. It forwards /worker/metrics
# to the Rust V2 worker's private metrics endpoint.
- targets:
    - 10.90.0.11:9188
  labels:
    client: acme
    environment: production
    service: worker
