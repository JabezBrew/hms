# Railway configuration for HMS Backend API
# Using Nixpacks builder (auto-detects Django)

[build]
builder = "nixpacks"

[deploy]
numReplicas = 1
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
# startCommand removed - set per-service in Railway UI
