# Keep frontend lib modules when deployment tooling packages `frontend/`
# as its own repository root and re-evaluates ignore patterns there.
!src/lib/
!src/lib/**
!src/shared/lib/
!src/shared/lib/**
